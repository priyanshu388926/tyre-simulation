# Python
__pycache__/
*.py[cod]
*.pyo
*.pyd
*.egg-info/
dist/
build/

# Virtual environments
.venv/
venv/
env/

# OS
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/
*.swp

# Streamlit
.streamlit/secrets.toml

# Generated reports (not committed — downloaded by users)
reports/
*.pdf

# Fast-load CSV is generated by train_models.py — commit it too
# data/apollo.csv  ← intentionally NOT ignored

# ─── Artifacts ────────────────────────────────────────────────────────────────
# Trained model artifacts ARE committed to git so Render can load them.
# Uncomment the lines below ONLY if you want to exclude them (not recommended
# for Render free tier which cannot run train_models.py in build time).
#
# artifacts/*.joblib
# artifacts/report.json
# artifacts/feature_cols.json
